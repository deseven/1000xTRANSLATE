{
  "name": "sort-dialogues",
  "version": "1.0.0",
  "description": "sorts existing dialogue keys in the storage into the natural conversation order",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "deseven",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
